<head>
    <title> </title>
</head>
<h2> Page of levels!</h2>

<% if !@levels.blank? then %>
<p> <%=@levels.class%></p>
<%@levels.each do |level| %>

<ul> 
    <li><%=link_to("Level #{level.id}", level_path)%></li>
    
    <ul>
<%end%>
<%else%>
    <span> there are no levels at the moment! </span>

<%end%>